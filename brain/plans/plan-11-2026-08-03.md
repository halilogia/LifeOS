# Plan 11 — AiSettingsTab.tsx Bölme (660 → ~90 satır)

**Tarih**: 2026-08-03
**Dosya**: [src/components/settings/AiSettingsTab.tsx](file:///c:/Users/emre_/Desktop/GitHub/Done/chrome-extension/src/components/settings/AiSettingsTab.tsx)
**Limit**: View ≤300 satır (§6.1)

---

## Problem

`AiSettingsTab.tsx` **660 satır** — limitin 2.2 katı. 3 bağımsız bölüm:

| Bölüm | Satır | Sorumluluk |
|---|---|---|
| AI API yapılandırma formu | 153-483 | key, endpoint, model seçici + fetch + gruplama |
| Thinking toggle | 484-521 | düşünce gösterme anahtarı |
| Kişisel hafıza (memory.md) | 523-655 | textarea + kaydet |
| State + mantık | 28-152 | models fetch, memory yükleme, gruplama |

## Hedef Mimari (Tuval + Parçalar)

```
src/components/settings/ai/
├── AiSettingsTab.tsx        → tuval (~90 satır) — state + kompozisyon
├── AiConfigForm.tsx         → parça (~280 satır) — key/endpoint/model formu
├── AiThinkingToggle.tsx     → parça (~40 satır) — düşünce toggle
└── AiMemoryEditor.tsx       → parça (~130 satır) — memory.md düzenleyici
```

## Yeni Dosyalar

### [NEW] `src/components/settings/ai/AiConfigForm.tsx` (~280 satır)
Mevcut L153-483'ü taşır + model seçici mantığı:
- Provider/kullanıcı satırı (L157-182)
- API Key input + göz göster/gizle (L184-280)
- Endpoint input (L282-323)
- Model seçici: fetch butonu, filtre, gruplu select, aktif ID (L325-482)
- Props: `t`, `aiApiKey`, `aiModel`, `aiEndpoint`, `showKey`, `models`, `filterText`, `filteredModels`, `groupedModels`, `loadingModels`, `modelError`, `onToggleKey`, `onFilterChange`, `onConfigChange`, `onFetchModels`

> ⚠️ 280 satır view limitine (300) yakın — sınırda kalır. `prefixMap` + `groupedModels` hesaplaması tuvalde kalırsa form ~250'ye iner.

### [NEW] `src/components/settings/ai/AiThinkingToggle.tsx` (~40 satır)
Mevcut L484-521'i taşır:
- Props: `t`, `aiShowThinking`, `onToggle`

### [NEW] `src/components/settings/ai/AiMemoryEditor.tsx` (~130 satır)
Mevcut L523-655'i taşır:
- Başlık + kayıt rozeti, açıklama, textarea, kaydet butonu
- Props: `t`, `userMemory`, `memorySavedSuccess`, `onMemoryChange`, `onSaveMemory`

## Değişen Dosyalar

### [MODIFY] `src/components/settings/AiSettingsTab.tsx` (660 → ~90 satır)
Tuval — sadece:
- State: `showKey`, `models`, `loadingModels`, `modelError`, `userMemory`, `memorySavedSuccess`, `filterText` (L28-33, 114)
- Effect'ler: memory yükleme + listener (L35-66), models fetch (L110-112)
- `handleSaveMemory` (L68-73), `fetchModels` (L75-108)
- Türetilmiş: `filteredModels`, `prefixMap`, `groupedModels` (L116-151)
- 3 parçanın kompozisyonu

## Korunacaklar (Zero-Loss §5.5)

1. **Memory yükleme**: default memory metni + storage.onChanged listener — aynen
2. **Models fetch**: OpenRouter default endpoint + Bearer header + hata yönetimi — aynen
3. **Gruplama**: `prefixMap` (oc, cx, gc, mc, kc, openrouter, ollama, gemini, gcli) — aynen
4. **Key göster/gizle**: password/text toggle + göz ikonu — aynen
5. **i18n**: tüm `t.settings_ai_*` anahtarları aynen
6. **Inline stiller**: birebir korunur

## Doğrulama

```
npx tsc --noEmit
npx eslint src --quiet
npm run build
node scripts/findDeadFiles.mjs   → "Toplam: 0 dosya"
```

Ardından: `src/ARCHITECTURE.md` güncelle (§7.1) + mermaid diyagram (§7.2).

## Açık Sorular

> [!NOTE]
> `prefixMap` + `groupedModels` hesaplaması tuvalde mi kalsın yoksa AiConfigForm'a mı taşınsın? Form'a taşınırsa form ~280 satır olur (sınırda). Tuvalde kalırsa form ~250, tuval ~110 — ikisi de limit içinde. Öneri: tuvalde kalsın.
