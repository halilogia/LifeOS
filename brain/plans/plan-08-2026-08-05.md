# Plan 08 — Günlüğüm İyileştirmeleri (2026-08-05)

## Kapsam
NotesView (Günlüğüm) 4 iyileştirme.

## 1. ".md Olarak İndir" → SVG indirme ikonu
- `NoteCard.tsx` footer: metin butonu → kompakt **download SVG ikonu** buton.
- Label kaldır, sadece ikon; `title` tooltip'te kalır.

## 2. Copy butonu (SVG)
- `NoteCard.tsx` footer'a **copy SVG ikonu** butonu — `navigator.clipboard.writeText(note.content)`.
- Markdown ham metni kopyalar. SVG ikonu, emoji değil.

## 3. "Yeni Not" → direk yazmaya başla
- `NoteEditorBody` normal textarea ve `NoteEditorModal`'a `autofocus` + ref atanıp
  modal açılınca content textarea'ya focus verilir. Başlık anahtarı açıkken kullanıcı
  hemen "Bir şeyler yazın..." alanına yazar.

## 4. Güne Puan / Mood Tracker
- Yeni storage key: `SYNC_DAY_SCORES = "dayScores"` (`dayScores: Record<string, number>`, key = `YYYY-MM-DD`, değer 1-10).
- `keys.ts` + `SYNC_ALL_KEYS`'a eklenir (cloud sync).
- Yeni bileşen `NotesDayScorePanel.tsx` (notes/):
  - Bugünün puanı için 1-10 nokta/yıldız seçici (SVG, renk KODLU gradient — emoji yok).
  - Altında son 7 gün mini ısı şeridi (renkli dot = mood tracker görünümü).
- `useNotes` hook'unda `dayScores` state + `setDayScore(date, score)` (chrome.storage.sync).
- NotesView başlık altına yerleşir.
- i18n `tr`/`en`: `notes_day_score_label`, tooltip'ler.

## Dosyalar
- `src/types/types.ts` (+DayScore tip), `src/infrastructure/storage/keys.ts`
- `src/presentation/hooks/useNotes.ts`, `src/components/notes/NotesDayScorePanel.tsx` [NEW]
- `src/components/notes/NoteCard.tsx`, `src/components/notes/NoteEditorModal.tsx`, `NoteEditorBody.tsx`
- `src/components/NotesView.tsx`, `src/css/newtab/notes.css`
- `src/utils/translations/tr/notes.ts`, `en/notes.ts`

## Doğrulama
`npx tsc --noEmit` + `npm run build` + `node scripts/findDeadFiles.mjs` (0) + prettier + eslint.
ARCHITECTURE.md + task.md güncellenir.