# Plan 09 — arcadeService.ts Bölme (761 → ~120 satır)

**Tarih**: 2026-08-03
**Dosya**: [src/services/arcadeService.ts](file:///c:/Users/emre_/Desktop/GitHub/Done/chrome-extension/src/services/arcadeService.ts)
**Limit**: Service ≤200 satır (§6.1)

---

## Problem

`arcadeService.ts` **761 satır** — limitin 3.8 katı. İçerik 4 farklı sorumluluk:

| Bölüm | Satır | Sorumluluk |
|---|---|---|
| Dosya sistemi helper'ları (ensurePermission, base64, mime, walk, cover, entry) | 4-274 | File System Access API |
| HTML yeniden yazma (rewriteHTML, rewriteAttributes, isExternal, joinPath) | 275-370 | Vite build HTML dönüşümü |
| Public API (createArcadeService + 10 metod) | 371-734 | Repo orchestration |
| Misc (hasDirectoryNamed, singleton) | 736-761 | Yardımcı + instance |

## Hedef Mimari

```
src/services/arcade/
├── index.ts              → barrel export (~15 satır) — arcadeService singleton + ArcadeService tip
├── types.ts              → GamePackage, ImportMode, ImportResult (~20 satır)
├── arcadeFileSystem.ts   → dosya sistemi helper'ları (~140 satır)
├── arcadeHtmlRewriter.ts → HTML yeniden yazma (~100 satır)
├── arcadeService.ts      → createArcadeService + 10 metod (~250 satır)
└── arcadeRepository.ts   → hasDirectoryNamed (~10 satır) — veya arcadeService içinde kalır
```

## Yeni Dosyalar

### [NEW] `src/services/arcade/arcadeFileSystem.ts` (~140 satır)
Mevcut L4-274'ü taşır (tamamen kopyala, değiştirme):
- `DirHandle`, `FileHandle` interface'leri
- `ensurePermission`, `arrayBufferToBase64`, `getMimeType`, `formatFolderName`, `detectCategory`, `detectTechStack`
- `isDirHandle`, `isFileHandle`, `walkDirectory`
- `findCoverImage`, `findEntryHTMLPath`
- Import: `GameEntry` tipi (`@/types/game.js`), `logger`

### [NEW] `src/services/arcade/arcadeHtmlRewriter.ts` (~100 satır)
Mevcut L275-370'i taşır (kopyala):
- `isExternal`, `joinPath`, `rewriteHTML`, `rewriteAttributes`
- **Export**: `rewriteHTML` (iç kullanım)

### [NEW] `src/services/arcade/types.ts` (~20 satır)
Mevcut L375-386:
- `GamePackage`, `ImportMode`, `ImportResult` interface'leri

### [NEW] `src/services/arcade/index.ts` (~15 satır)
- `export { arcadeService } from "./arcadeService.js";`
- `export type { ArcadeService } from "./arcadeService.js";`
- `export * from "./types.js";`

## Değişen Dosyalar

### [MODIFY] `src/services/arcade/arcadeService.ts` (761 → ~250 satır)
Mevcut `src/services/arcadeService.ts` içeriği taşınır:
- Import: `arcadeFileSystem.js` helper'ları + `arcadeHtmlRewriter.js` + `types.js`
- `createArcadeService` + 10 metod aynen korunur
- `hasDirectoryNamed` burada kalır (sadece 1 yerde kullanılıyor)
- `ChromeStorageArcadeRepository` import + `arcadeService` singleton burada kalır

> ⚠️ 250 satır service limitini (200) aşar. Metodlar repo çağrısı + state değişikliği — bölünemez (hepsi `repo.getAllGames()` + `saveAllGames` deseni). `resolveGameURL` (~110 satır) en büyük — `arcadeGameLauncher.ts` servisine taşınırsa ana dosya ~140'a iner. Karar plan onayında.

### [DELETE] `src/services/arcadeService.ts`
İçerik yeni klasöre taşınınca eski dosya silinir (§6.4 dead file kuralı).

## Import Güncellemeleri

Mevcut `arcadeService` import'ları (`@/services/arcadeService.js`):
```
src/components/ArcadeView.tsx
src/components/arcade/ArcadeGameCard.tsx? (kontrol edilecek)
src/components/arcade/ArcadeGameModal.tsx? (kontrol edilecek)
```
→ `@/services/arcade/index.js` (veya `arcadeService.js` barrel path'i)

## Korunacaklar (Zero-Loss §5.5)

1. **10 metod davranışı aynen**: loadGames, importFolder, loadGamePackage, resolveGameURL, resolveGameHTML, deleteGame, toggleFavorite, updateGameStatus, updateDevNotes, recheckGame, ensurePermissionForGame, listGamesWithAccess
2. **Güvenlik kontrolleri**: `ALLOWED_ENTRY_PATHS`, `..` path engeli, meta tag temizleme, inline event kaldırma — aynen
3. **Hata logları**: `logger.warn` çağrıları aynen
4. **Singleton**: `arcadeService` export'u aynı isimle korunur (import kırılmasın)
5. `GameEntry` tip şekli değişmez

## Doğrulama

```
npx tsc --noEmit
npx eslint src --quiet
npm run build
node scripts/findDeadFiles.mjs   → "Toplam: 0 dosya" (eski arcadeService.ts silinecek)
```

Ardından: `src/ARCHITECTURE.md` güncelle (§7.1) + mermaid diyagram (§7.2).

## Açık Sorular

> [!IMPORTANT]
> `resolveGameURL` (~110 satır) tek başına service limitinin yarısı. Ayrı `arcadeGameLauncher.ts` dosyasına taşıyayım mı? Taşırsam ana service ~140 satıra iner, tam uyumlu olur. Taşımazsam 250 satır kalır (istisna).

> [!NOTE]
> `src/components/arcade/` içindeki import'lar taşınmadan önce taranacak — `arcadeService.js` path'ini kullanan tüm dosyalar güncellenecek.
