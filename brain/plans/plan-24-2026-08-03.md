# Plan 24 — Kalan Servis Bölmeleri (ambientAudio, kpssAi, bist, ipo, arcade 2. tur)

**Tarih**: 2026-08-03
**Dosya**: ambientAudioService (366), kpssAiService (338), bistService (336), ipoService (297), arcadeService 2. tur (282), stockAiService (277)
**Kural**: §6.1 Sorumluluk Esaslı

---

## Problem

6 servis çok sorumluluk:

| Servis | Satır | Sorumluluklar |
|---|---|---|
| ambientAudioService | 366 | ses üretimi + audio context + çalma listesi |
| kpssAiService | 338 | AI quiz + konu önerisi + analiz |
| bistService | 336 | ticker fetch + quote + hisse arama + haberler |
| ipoService | 297 | IPO listesi + detay + takvim |
| arcadeService (2. tur) | 282 | repo orchestration (bölündü ama hâlâ yoğun) |
| stockAiService | 277 | AI yorum + rapor + prompt'lar |

## Hedef Mimari

```
src/services/ambient/
├── ambientAudioService.ts → tuval (~120) — ana API
├── audioEngine.ts         → parça (~130) — Web Audio context + ses üretimi
└── ambientTracks.ts       → parça (~60) — çalma listesi

src/services/kpss/
├── kpssAiService.ts       → tuval (~140) — AI istekleri
├── kpssAiPrompts.ts       → parça (~90) — prompt şablonları
└── kpssAiParsers.ts       → parça (~70) — yanıt ayrıştırma

src/services/bist/
├── bistService.ts         → tuval (~140) — ticker + quote API
├── bistTickerSource.ts    → parça (~80) — dinamik ticker kaynakları
└── bistNewsFetcher.ts     → parça (~70) — haber çekme

src/services/arcade/ (2. tur)
├── arcadeService.ts       → ~160'a iner (importFolder + repo metodları ayrılır)
└── arcadeImporter.ts      → parça (~110) — importFolder mantığı
```

## Korunacaklar
- Web Audio context davranışı — aynen
- Tüm API endpoint'leri + yanıt tipleri — aynen
- AI prompt formatları — aynen
- arcade importFolder akışı — aynen

## Doğrulama
`npx tsc --noEmit` → `npx eslint src --quiet` → `npm run build` → `node scripts/findDeadFiles.mjs`
