# Plan 23 — AiChatMessageItem + useAiChatMessages + PopupDetoxTab Bölme

**Tarih**: 2026-08-03
**Dosya**: AiChatMessageItem.tsx (415), useAiChatMessages.ts (406), PopupDetoxTab.tsx (347)
**Kural**: §6.1 Sorumluluk Esaslı

---

## Problem

**AiChatMessageItem (415):** 7 ikon + mesaj render (çeşitli tipler) + kopyala/arama aksiyonları
**useAiChatMessages (406):** hook — sohbet state + borsa verisi bağlamı (L156) + AI çağrıları. HOOK ama borsa bağlamı ayrı sorumluluk.
**PopupDetoxTab (347):** durum satırı + süre seçici + platform ızgarası (L67-239)

## Hedef Mimari

```
src/components/aichat/
├── AiChatMessageItem.tsx → tuval (~180) — mesaj tipi routing
├── aichatIcons.tsx       → parça (~120) — 7 ikon
├── MessageActions.tsx    → parça (~60) — kopyala/arama butonları
├── useAiChatMessages.ts  → tuval hook (~250) — sohbet mantığı
└── bistContextBuilder.ts → parça (~80) — borsa bağlamı string builder

src/components/popup/
├── PopupDetoxTab.tsx     → tuval (~120) — state + 3 parça
├── DetoxStatusRow.tsx    → parça (~50) — durum
├── DetoxDurationSelect.tsx → parça (~60) — süre
└── DetoxPlatformGrid.tsx → parça (~90) — platformlar + ikonlar
```

## Korunacaklar
- Mesaj tipleri render + kopyalama + kaynak linkleri — aynen
- Borsa bağlamı string formatı — aynen
- Detox platform ızgarası + süre seçimi + aktif durum — aynen

## Doğrulama
`npx tsc --noEmit` → `npx eslint src --quiet` → `npm run build` → `node scripts/findDeadFiles.mjs`
