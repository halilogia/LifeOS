# Plan 19 — ArcadeGameModal.tsx Bölme (515 → ~150 satır)

**Tarih**: 2026-08-03
**Dosya**: [src/components/arcade/ArcadeGameModal.tsx](file:///c:/Users/emre_/Desktop/GitHub/Done/chrome-extension/src/components/arcade/ArcadeGameModal.tsx)
**Kural**: §6.1 Sorumluluk Esaslı

---

## Problem

`ArcadeGameModal.tsx` **515 satır**, 2 tab + toast + iframe yönetimi:
- State: tab (play/dev), toast, iframe ref, messageSent ref (L22-85)
- Effect'ler: oyun yükleme + mesaj gönderme (L86-223)
- JSX: oyun iframe'i + dev notları + todo + toast (L224-515)

## Hedef Mimari

```
src/components/arcade/
├── ArcadeGameModal.tsx   → tuval (~180) — state + iframe + tab routing
├── ArcadePlayTab.tsx     → parça (~120) — oyun iframe'i + kontroller
├── ArcadeDevTab.tsx      → parça (~130) — dev notları + todoList + durum
└── ArcadeToast.tsx       → parça (~40) — toast bildirimi
```

## Korunacaklar
- iframe mesaj protokolü (messageSent ref) — aynen
- Oyun yükleme + hata toast'ları — aynen
- Dev tab: devNotes + todoList + title düzenleme — aynen
- Tab geçişleri — aynen

## Doğrulama
`npx tsc --noEmit` → `npx eslint src --quiet` → `npm run build` → `node scripts/findDeadFiles.mjs`
