# Plan 22 — KpssSettingsTab.tsx + AppSettingsGroup.tsx Bölme (401 + 392)

**Tarih**: 2026-08-03
**Dosya**: [KpssSettingsTab.tsx](file:///c:/Users/emre_/Desktop/GitHub/Done/chrome-extension/src/components/settings/KpssSettingsTab.tsx) + [AppSettingsGroup.tsx](file:///c:/Users/emre_/Desktop/GitHub/Done/chrome-extension/src/components/settings/AppSettingsGroup.tsx)
**Kural**: §6.1 Sorumluluk Esaslı

---

## Problem

İki settings bileşeni şişkin, her biri çok sayıda ayar grubu:

**KpssSettingsTab (401):**
- Hedef tipi seçici (L53) + hedef ayar kontrolleri (L119) + wiki notları (L248) + veri sıfırlama (L343)

**AppSettingsGroup (383):**
- Dil + bildirimler (free games/calendar/pomodoro/infobox) + WhatsApp/Telegram köprüsü + tab gruplama + hotkey yönetimi (L68-344)

## Hedef Mimari

```
src/components/settings/kpss/
├── KpssSettingsTab.tsx    → tuval (~150) — 4 grup kompozisyonu
├── KpssGoalSelector.tsx   → parça (~50) — hedef tipi pill'leri
├── KpssTargetControls.tsx → parça (~80) — hedef ayarları
├── KpssWikiSettings.tsx   → parça (~70) — wiki notları ayarları
└── KpssResetSection.tsx   → parça (~60) — veri sıfırlama

src/components/settings/app/
├── AppSettingsGroup.tsx   → tuval (~100) — gruplar
├── AppToggleRow.tsx       → parça (~50) — tek toggle satırı (yeniden kullanılabilir)
├── AppLanguageRow.tsx     → parça (~40) — dil seçici
└── AppHotkeyPanel.tsx     → parça (~90) — infobox + copilot hotkey
```

## Korunacaklar
- Tüm ayar yazma akışları (storage) — aynen
- Hotkey yönetimi + toggle davranışları — aynen
- Reset KPSS verisi onay modalı — aynen

## Doğrulama
`npx tsc --noEmit` → `npx eslint src --quiet` → `npm run build` → `node scripts/findDeadFiles.mjs`
