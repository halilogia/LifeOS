# Plan 08 — SidePanelApp.tsx Bölme (1082 → ~250 satır)

**Tarih**: 2026-08-03
**Dosya**: [src/sidepanel/SidePanelApp.tsx](file:///c:/Users/emre_/Desktop/GitHub/Done/chrome-extension/src/sidepanel/SidePanelApp.tsx)
**Limit**: View ≤300 satır (§6.1)

---

## Problem

`SidePanelApp.tsx` **1082 satır** — limitin 3.6 katı. İçinde:
- State yönetimi (chat, ses, sekme bağlamı)
- AI API çağrıları (Gemini + OpenAI-uyumlu)
- Sayfa form algılama mantığı (`isPersonalFormPage`)
- 5 chip prompt yönlendirici
- 400 satır JSX (header, tab info, chips, mesajlar, input)

## Hedef Mimari (Tuval + Hook + Parçalar)

```
src/sidepanel/
├── SidePanelApp.tsx          → tuval (~250 satır) — sadece state + kompozisyon
├── useSidePanelChat.ts       → hook (~280 satır) — tüm iş mantığı
├── SidePanelHeader.tsx       → parça (~70 satır) — başlık + yeni sohbet
├── SidePanelTabBar.tsx       → parça (~90 satır) — aktif sekme bilgisi + yenile
├── SidePanelChips.tsx        → parça (~80 satır) — 6 chip + YouTube varyantı
├── SidePanelMessages.tsx     → parça (~120 satır) — mesaj listesi + kopyala + boş durum
├── SidePanelInputBar.tsx     → parça (~70 satır) — metin girişi + ses + gönder
└── ChatMessage.ts            → tip (~15 satır) — ChatMessage interface
```

## Yeni Dosyalar

### [NEW] `src/sidepanel/useSidePanelChat.ts` (~280 satır)
Hook — mevcut `SidePanelApp` içindeki **tüm state ve mantığı** taşır:
- State: `messages`, `inputText`, `isProcessing`, `agentStatus`, `pageContext`, `isListening`, `activeSessionKey`, `lang`
- `toggleVoiceInput()` — SpeechRecognition (L114-156)
- `refreshPageContext()` — sekme bağlamını getir + session key (L218-266)
- `handleNewChat()` — sohbeti sıfırla (L268-273)
- `handleSendMessage(promptOverride?)` — AI çağrısı + JSON aksiyon ayrıştırma (L333-636)
- `handleChipClick(type)` — chip prompt'ları (L640-664)
- `isYoutube` türetilmiş değeri
- Effect'ler: başlangıç yükleme + listener'lar (L161-216)
- **Dönüş**: tüm state + handler'lar + `isYoutube`

> ⚠️ 280 satır hook için 200-limit aşımı — `useKpssNotes` (253) ile aynı istisna; mantık bölünemeyecek kadar birbirine bağlı (sendMessage 300 satır tek akış). Alternatif: `aiChatRequest.ts` servisine API çağrısını çıkarırsak ~200'e iner.

### [NEW] `src/sidepanel/SidePanelHeader.tsx` (~70 satır)
- L669-723: header + SVG ikon + başlık + `AI Agent` rozeti
- Yeni sohbet butonu (`handleNewChat`)

### [NEW] `src/sidepanel/SidePanelTabBar.tsx` (~90 satır)
- L724-763: aktif sekme başlığı + URL + yenile butonu
- Props: `t`, `pageContext`, `onRefresh`

### [NEW] `src/sidepanel/SidePanelChips.tsx` (~80 satır)
- L764-923: 6 chip (özetle, anahtar çıkarımlar, sor, çıkar, YouTube özeti/quiz)
- YouTube varyantı (`isYoutube` true ise normal chiplear yerine YT chiplearı)
- Props: `t`, `isYoutube`, `onChipClick`

### [NEW] `src/sidepanel/SidePanelMessages.tsx` (~120 satır)
- L924-1020: mesaj listesi, boş durum (ai-orb + starter kartlar), kopyalama butonu
- `SidePanelCopyBtn` bileşenini içine taşır (L275-331)
- Props: `t`, `messages`, `agentStatus`, `onNewChat` (starter kartlar için)

### [NEW] `src/sidepanel/SidePanelInputBar.tsx` (~70 satır)
- L1021-1081: mikrofon butonu + input + gönder
- Props: `t`, `inputText`, `isProcessing`, `isListening`, `onInputChange`, `onSend`, `onToggleVoice`

### [NEW] `src/sidepanel/ChatMessage.ts` (~15 satır)
- `ChatMessage` interface (L15-20) — ayrı tip dosyası

## Değişen Dosyalar

### [MODIFY] `src/sidepanel/SidePanelApp.tsx` (1082 → ~250 satır)
- `isPersonalFormPage()` fonksiyonunu **koru** (kullanılıyor mu kontrol et — L25-100; kullanılmıyorsa taşı/sil kararı plan onayında netleşir)
- Tuval: sadece `useSidePanelChat()` hook çağrısı + 5 parçanın kompozisyonu
- Tüm iş mantığı hook'a taşınır

## Korunacaklar (Zero-Loss §5.5)

1. **API akışı**: Gemini + OpenAI-uyumlu çağrılar, JSON ayrıştırma, memory güncelleme, agent aksiyon yürütme — aynen
2. **Session persistence**: Tab Group'a göre `chrome.storage.local` anahtarı
3. **Listener'lar**: tabActivated, tabUpdated, copilot_auto_prompt — aynı sırayla ekle/çıkar
4. **Chip prompt'ları**: 6 çeviri anahtarı aynen
5. **Hata yönetimi**: try/catch + `⚠️` hata mesajı formatı
6. **i18n**: tüm `t.*` anahtarları aynen
7. `ChatMessage` shape'i değişmez

## Doğrulama

```
npx tsc --noEmit
npx eslint src --quiet
npm run build
node scripts/findDeadFiles.mjs   → "Toplam: 0 dosya" (eski dosya silinmez, sadece içerik taşınır; SidePanelApp kalır)
```

Ardından: `src/ARCHITECTURE.md` güncelle (§7.1) + mermaid diyagram (§7.2).

## Açık Sorular

> [!IMPORTANT]
> `isPersonalFormPage()` (L25-100, ~75 satır) kullanılıyor mu? Taramada sadece tanımı göründü, çağrı yeri görünmedi. Kullanılmıyorsa taşımak yerine silmek en temizi — ama §5.5 zero-loss gereği silmeden önce onayın gerekli.

> [!NOTE]
> `useSidePanelChat.ts` hook'u ~280 satır olacak (200 limiti aşar). `aiChatRequest.ts` servisine API çağrısını (L433-514) çıkararak ~200'e indirebilirim. Hangisini tercih edersin?
