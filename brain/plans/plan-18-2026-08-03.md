# Plan 18 — NoteCard.tsx + NoteEditorModal.tsx Bölme (500 + 474)

**Tarih**: 2026-08-03
**Dosya**: [NoteCard.tsx](file:///c:/Users/emre_/Desktop/GitHub/Done/chrome-extension/src/components/notes/NoteCard.tsx) + [NoteEditorModal.tsx](file:///c:/Users/emre_/Desktop/GitHub/Done/chrome-extension/src/components/notes/NoteEditorModal.tsx)
**Kural**: §6.1 Sorumluluk Esaslı

---

## Problem

İki not bileşeni şişkin:
- **NoteCard (500)**: kart görünümü + zettelkasten tag/link çipleri + markdown içerik render (L50-363) + dışa aktarma metni (L464)
- **NoteEditorModal (474)**: düzenleyici form + autocomplete popup (L349) + backlinks paneli (L409)

## Hedef Mimari

```
src/components/notes/
├── NoteCard.tsx            → tuval (~180) — kart + aksiyonlar
├── NoteContentRenderer.tsx → parça (~150) — markdown/içerik render
├── NoteTagChips.tsx        → parça (~70) — zettelkasten çipleri
├── NoteEditorModal.tsx     → tuval (~200) — form
├── WikiAutocomplete.tsx    → parça (~80) — [[ autocomplete
└── NoteBacklinksPanel.tsx  → parça (~90) — bağlantılı notlar
```

## Korunacaklar
- Markdown render mantığı (mdText oluşturma, tarih satırı) — aynen
- Tag/link çipleri tıklama davranışı — aynen
- Autocomplete ekleme akışı — aynen
- Backlinks paneli — aynen

## Doğrulama
`npx tsc --noEmit` → `npx eslint src --quiet` → `npm run build` → `node scripts/findDeadFiles.mjs`
