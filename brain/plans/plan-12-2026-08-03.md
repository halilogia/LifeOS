# Plan 12 — aiChatService.ts Bölme (545 → ~60 satır)

**Tarih**: 2026-08-03
**Dosya**: [src/services/aiChatService.ts](file:///c:/Users/emre_/Desktop/GitHub/Done/chrome-extension/src/services/aiChatService.ts)
**Limit**: Service ≤200 satır (§6.1)

---

## Problem

`aiChatService.ts` **545 satır** — limitin 2.7 katı. İçerik 4 sorumluluk:

| Bölüm | Satır | Sorumluluk |
|---|---|---|
| Tipler (AICallParams, AIResponseData, AiChatDependencies) | 25-49 | Arayüz kontratları |
| buildSystemPrompt | 60-128 | System prompt + web arama + memory bağlam |
| callOllama / callOpenRouter / callGemini / callAIConfigured | 131-367 | 3 farklı API sağlayıcı çağrısı |
| executeAIAction / handleAddNoteFromAI / handleUpdateMemoryFromAI | 368-474 | Aksiyon yürütme |
| Lazy singleton + module fonksiyonları | 487-545 | Instance yönetimi |

## Hedef Mimari

```
src/services/aichat/
├── index.ts               → barrel (~15 satır) — lazy singleton + modül fonksiyonları
├── types.ts               → AICallParams, AIResponseData, AiChatDependencies (~40)
├── systemPrompt.ts        → buildSystemPrompt (~70)
├── providers.ts           → callOllama, callOpenRouter, callGemini, callAIConfigured (~220)
└── actions.ts             → executeAIAction, handleAddNoteFromAI, handleUpdateMemoryFromAI (~110)
```

## Yeni Dosyalar

### [NEW] `src/services/aichat/types.ts` (~40 satır)
Mevcut L25-49'u taşır:
- `AICallParams`, `AIResponseData`, `AiChatDependencies` interface'leri
- `WebSearchSource` import burada (bazı tipler providers/actions'ta kullanılır)

### [NEW] `src/services/aichat/providers.ts` (~200 satır)
Mevcut L131-303'ü taşır (4 provider fonksiyonu: Ollama, OpenRouter, Gemini, callAIConfigured):
- `logger`, `parseAIResponse` import'ları buraya taşınır
- 200 satır limitine yakın — sınırda kalır

### [NEW] `src/services/aichat/systemPrompt.ts` (~70 satır)
Mevcut L60-128'i taşır:
- `buildSystemPrompt` — web arama + memory bağlamı + system prompt şablonu
- `executeWebSearch`, `detectNeedsWebSearch`, `WebSearchSource` import'ları

### [NEW] `src/services/aichat/actionExecutor.ts` (~110 satır)
Mevcut L368-474'ü taşır:
- `executeAIAction`, `handleAddNoteFromAI`, `handleUpdateMemoryFromAI`
- todoRepo/noteRepo/memoryRepo kullanır (deps parametreyle geçer)

### [NEW] `src/services/aichat/index.ts` (~60 satır)
- `createAiChatService(deps)` — `createAiChatServiceFactory` (L51-483)
- Lazy singleton `getAiChatService()` (L499-509)
- Modül fonksiyonları: `getAIConfigFromStorage`, `callAIConfigured`, `executeAIAction`, `handleAddNoteFromAI`, `handleUpdateMemoryFromAI` (L511-544)
- Infra import'ları burada (4 ChromeStorage repo) + `AiChatService` tip export

## Değişen Dosyalar

### [MODIFY] `src/services/aichat/aiChatService.ts`? — HAYIR

> **Karar**: Ana mantık mevcut `aiChatService.ts` dosyasında kalabilir ama sanıyorum en temiz yol: eski `src/services/aiChatService.ts` SİLİNİR, tüm içerik `src/services/aichat/` klasörüne taşınır. Barrel `index.ts` tüm export'ları aynı isimle verir — import kırılmaz.

### [DELETE] `src/services/aiChatService.ts`
İçerik yeni klasöre taşınınca silinir (§6.4).

## Import Güncellemeleri

Mevcut `aiChatService.js` import'ları taranacak (`@/services/aiChatService.js`):
```
src/services/architecture üzeri birçok component + background + content
```
→ `@/services/aichat/index.js` (barrel path)

## Korunacaklar (Zero-Loss §5.5)

1. **4 provider davranışı aynen**: Ollama, OpenRouter, Gemini — endpoint/header/body aynen
2. **System prompt metni**: JSON şema + Türkçe varsayılan + web citation kuralı aynen
3. **Memory güncelleme**: `## 💡 AI Tarafından Öğrenilen Bilgiler` mantığı aynen
4. **Aksiyon yürütme**: create_task/add_note/update_memory şubeleri aynen
5. **Lazy singleton**: `getAiChatService()` lazy init aynen (infra import'ta yaratılmaz)
6. **Modül fonksiyon imzaları**: `getAIConfigFromStorage`, `callAIConfigured` vb. aynen — import kırılmasın

## Doğrulama

```
npx tsc --noEmit
npx eslint src --quiet
npm run build
node scripts/findDeadFiles.mjs   → "Toplam: 0 dosya"
```

Ardından: `src/ARCHITECTURE.md` güncelle (§7.1) + mermaid diyagram (§7.2).

## Açık Sorular

> [!WARNING]
> `providers.ts` 200 satır limitine çok yakın (~200). `callAIConfigured` (L303-367, ~65 satır) orchestrator — ayrı `caller.ts`'e alınabilir ama 4 provider + orchestrator aynı dosyada kalırsa ~200'e çıkar. Kabul edilebilir (sınırda). Yoksa `callAIConfigured`'ı ayrı dosyaya alırım.

> [!NOTE]
> `aiChatService`'i kimler import ediyor — sayı az mı çok mu? Çok import varsa barrel `index.ts` şart. Taranacak.
