# Plan 25 — Kalan Düşük Öncelik Bölmeler (opsiyonel)

**Tarih**: 2026-08-03
**Dosya**: ZettelkastenGraphModal (487), KpssQuizResultStep (344), CustomStockChart (333), StockWatchlistTable (323), StockKapNewsModal (377), PomoTimerCard (318), KpssWikiSidebar (303)
**Kural**: §6.1 Sorumluluk Esaslı

---

## Problem

Kalan şişkin dosyalar — her biri 1 ana sorumluluk ama büyük UI blokları.

## Önerilen bölmeler

| Dosya | Satır | Ayrılacak parça |
|---|---|---|
| ZettelkastenGraphModal | 487 | `GraphSvgCanvas.tsx` (~200) — SVG node/edge render |
| KpssQuizResultStep | 344 | `QuizResultHero.tsx` (~40) + `QuizReviewList.tsx` (~120) |
| CustomStockChart | 333 | chart config + render ayrımı (`StockChartConfig.ts`) |
| StockWatchlistTable | 323 | `WatchlistRow.tsx` (~80) — satır aksiyonları |
| StockKapNewsModal | 377 | `KapNewsList.tsx` (~100) — haber listesi |
| PomoTimerCard | 318 | `PomoDurationEditor.tsx` (~60) |
| KpssWikiSidebar | 303 | `WikiNoteTree.tsx` (~120) — ağaç render |

## Korunacaklar
- SVG grafik düzeni + hover tooltip — aynen
- Quiz sonuç skor + soru inceleme — aynen
- Watchlist satır toggle + silme — aynen
- KAP haber listesi + dış link — aynen

## Doğrulama
`npx tsc --noEmit` → `npx eslint src --quiet` → `npm run build` → `node scripts/findDeadFiles.mjs`

> [!NOTE]
> Bu gruptakiler düşük öncelik — 1-4. turlar bitince yapılır. Her biri ayrı mini-plan istenirse oluşturulur.
