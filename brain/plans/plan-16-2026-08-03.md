# Plan 16 — App.tsx Bölme (418 → ~150 satır)

**Tarih**: 2026-08-03
**Dosya**: [src/App.tsx](file:///c:/Users/emre_/Desktop/GitHub/Done/chrome-extension/src/App.tsx)
**Kural**: §6.1 Sorumluluk Esaslı

---

## Problem

`App.tsx` **418 satır**, ana app — bölümler:
- Repo + migration kurulumu (L34-45)
- Global state'ler + effect'ler (L47-222)
- Layout: sidebar + input header + settings drawer + viewport + footer + modallar (L223-418)

## Hedef Mimari

```
src/
├── App.tsx              → tuval (~150) — state + layout kompozisyonu
├── components/AppTopHeader.tsx → parça (~60) — input header
├── components/AppMainViewport.tsx → parça (~70) — aktif view routing
└── components/AppFooterQuote.tsx → parça (~30) — footer
```

## Korunacaklar
- Tüm global state + useEffect'ler (172-222) tuvalde kalır
- SettingsDrawer + ConfirmModal + AlertModal kompozisyonu — aynen
- Sidebar + header + viewport + footer yerleşimi — aynen
- Vite base / file:// path davranışı — aynen

## Doğrulama
`npx tsc --noEmit` → `npx eslint src --quiet` → `npm run build` → `node scripts/findDeadFiles.mjs`

## Açık Sorular
> [!NOTE]
> App.tsx zaten 418 → ~150'ye inerse bölünmüş sayılır. Ama App zaten ana koordinatör — state'ler kalabalıksa `useAppState` hook'u da çıkarılabilir (opsiyonel).
