# Plan 10 — BistKesfetTab.tsx Bölme (729 → ~100 satır)

**Tarih**: 2026-08-03
**Dosya**: [src/components/stock/BistKesfetTab.tsx](file:///c:/Users/emre_/Desktop/GitHub/Done/chrome-extension/src/components/stock/BistKesfetTab.tsx)
**Limit**: View ≤300 satır (§6.1)

---

## Problem

`BistKesfetTab.tsx` **729 satır** — limitin 2.4 katı. İçerik:

| Bölüm | Satır | Sorumluluk |
|---|---|---|
| 5 ikon bileşeni (Plus, Chart, Sparkles, Bookmark, Check) | 28-113 | Inline SVG'ler |
| State + mantık (ticker fetch, infinite scroll, featured hesaplama) | 126-209 | Veri işleme |
| AI Featured kartlar | 213-395 | Öne çıkan hisse kartları |
| Ticker grid + aksiyon butonları | 396-621 | Hisse kartları |
| Watchlist modal | 623-725 | Takip listesi seçici |

## Hedef Mimari (Tuval + Parçalar)

```
src/components/stock/kesfet/
├── BistKesfetTab.tsx        → tuval (~100 satır) — state + kompozisyon
├── KesfetFeaturedCard.tsx   → parça (~150 satır) — AI öne çıkan kartlar
├── KesfetTickerCard.tsx     → parça (~150 satır) — hisse kartı + 4 buton
├── KesfetWatchlistModal.tsx → parça (~120 satır) — takip listesi seçici
└── kesfetIcons.tsx          → parça (~90 satır) — 5 SVG ikon
```

## Yeni Dosyalar

### [NEW] `src/components/stock/kesfet/kesfetIcons.tsx` (~90 satır)
Mevcut L28-113'ü taşır:
- `IconPlus`, `IconChart`, `IconSparkles`, `IconBookmark`, `IconCheck`
- `export` yapılır (şu an modül içi)

### [NEW] `src/components/stock/kesfet/KesfetFeaturedCard.tsx` (~150 satır)
Mevcut L213-395'i taşır:
- AI Haftalık Öne Çıkanlar köşesi (grid + skor etiketleri + tag'ler)
- Props: `t`, `featuredStocks` (featured hesaplaması tuvalde kalır — veri mantığı)

### [NEW] `src/components/stock/kesfet/KesfetTickerCard.tsx` (~150 satır)
Mevcut L396-621'i taşır:
- Hisse kartı (ticker, fiyat, değişim, şirket adı)
- 4 aksiyon butonu: watchlist, portfolio, chart, AI
- "Daha fazla yükle" butonu
- Props: `t`, `fullSym`, `quote`, `quoteMap`, `onWatchlistModal`, `onQuickAddStock`, `onOpenChart`, `onOpenAiModal`

### [NEW] `src/components/stock/kesfet/KesfetWatchlistModal.tsx` (~120 satır)
Mevcut L623-725'i taşır:
- Takip listesi seçici modal (listeler + ekleme durumu + onay işareti)
- Props: `t`, `watchlists`, `symbol`, `onToggle`, `onClose`

## Değişen Dosyalar

### [MODIFY] `src/components/stock/BistKesfetTab.tsx` (729 → ~100 satır)
Tuval — sadece:
- State: `watchlistModalSymbol`, `newListName`, `bistTickers`, `visibleCount`
- Effect'ler: ticker fetch (L137-143), infinite scroll (L163-174)
- Türetilmiş: `allTickers`, `filteredTickers`, `displayedTickers`, `featuredStocks` (L145-200)
- `handleCreateAndAdd` (L202-209)
- 4 parçanın kompozisyonu

## Korunacaklar (Zero-Loss §5.5)

1. **Infinite scroll mantığı**: başlangıç 24, scroll +18, buton +24 — aynen
2. **Featured sıralama**: momentum öncelikli + TL hacim sort — aynen
3. **Tag/etiket mantığı**: volume leader, strong momentum, positive trend, correction — aynen
4. **Modal davranışı**: overlay click kapatma, stopPropagation — aynen
5. **i18n anahtarları**: `t.stock_*` hepsi aynen
6. **Buton stilleri**: inline style'lar birebir korunur (tema token'ı yok bu bileşende)

## Doğrulama

```
npx tsc --noEmit
npx eslint src --quiet
npm run build
node scripts/findDeadFiles.mjs   → "Toplam: 0 dosya"
```

Ardından: `src/ARCHITECTURE.md` güncelle (§7.1) + mermaid diyagram (§7.2).

## Açık Sorular

> [!NOTE]
> `newListName` state'i tuvalde kalıyor ama `handleCreateAndAdd` sadece modal içinde kullanılıyor. İkisini de KesfetWatchlistModal'a taşımak daha temiz — ama `onCreateWatchlist` prop'u tuvalden geliyor. Modal'a taşımak prop sayısını azaltır, istersen öyle yaparım.
