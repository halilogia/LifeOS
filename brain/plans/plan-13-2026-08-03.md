# Plan 13 — TurkeyMapView.tsx Bölme (578 → ~80 satır)

**Tarih**: 2026-08-03
**Dosya**: [src/components/kpss/map/TurkeyMapView.tsx](file:///c:/Users/emre_/Desktop/GitHub/Done/chrome-extension/src/components/kpss/map/TurkeyMapView.tsx)
**Limit**: View ≤300 satır (§6.1)

---

## Problem

`TurkeyMapView.tsx` **578 satır** — limitin ~2 katı. Tek sorumluluk (Türkiye haritası eğitim görünümü) ama 4 presentational blok var:

| Bölüm | Satır | Sorumluluk |
|---|---|---|
| State + playback mantığı (play, step, reset, drag, fullscreen) | 17-170 | Oynatma + etkileşim |
| Başlık + Kontrol çubuğu (ileri/geri, reset, play, fullscreen) | 172-316 | Video oynatıcı kontrolleri |
| Konu seçici sidebar | 318-398 | Konu listesi |
| Harita SVG + pinler + lejant + bilgi kartı | 400-564 | SVG render + overlay'ler |

## Hedef Mimari (Tuval + Parçalar)

```
src/components/kpss/map/
├── TurkeyMapView.tsx          → tuval (~180 satır) — state + playback + kompozisyon
├── MapControls.tsx            → parça (~150 satır) — başlık + ileri/geri + play + fullscreen
├── MapTopicSidebar.tsx        → parça (~80 satır) — konu seçici
└── MapCanvas.tsx              → parça (~160 satır) — SVG + pinler + lejant + bilgi kartı
```

## Yeni Dosyalar

### [NEW] `src/components/kpss/map/MapControls.tsx` (~150 satır)
Mevcut L191-316'yı taşır
- Başlık + sayma badge + ileri/geri + reset + play + fullscreen
- Props: `t`, `selectedTopic`, `topicTitle`: string, `total`, `revealedCount`, `playing`, `isFullscreen`, `onStep`, `onReset`, `onPlayToggle`, `onToggleFullscreen`

### [NEW] `src/components/kpss/map/MapTopicSidebar.tsx` (~80)
Mevcut L280-398'i taşır
- Konu butonları (renk noktası + başlık)
- Props: `t`, `selectedTopic`, `onSelect`

### [NEW] `src/components/kpss/map/MapCanvas.tsx` (~160 satır)
Mevcut L400-564'ü taşır
- SVG harita: il sınırları + konu pinleri + lejant + bilgi kartı
- Pointer drag olayları tuvalden gelir (tuval props'la gönderir)
- Props: `t`, `selectedTopic`, `topicColor`, `legendKey`, `pins`, `revealedCount`, `currentIndex`, `isFullscreen`, `svgWrapRef`, `onPointerDown/Move/Up/Cancel`

## Değişen Dosyalar

### [MODIFY] `src/components/kpss/map/TurkeyMapView.tsx` (578 → ~180 satır)
Tuval:
- State: `selectedTopic`, `revealedCount`, `currentIndex`, `playing`, `isFullscreen`, timer/container refs
- Mantık: `clearTimer`, `stopPlayback`, `handleTopicChange`, `handlePlay`, `handleReset`, `handlePointerDown/Move/Up`, `handleStep`, `toggleFullscreen`
- `dragRef`, `svgWrapRef` — tuvalde kalır (2 kullanım)
- 3 parçanın kompozisyonu

> Not: `TOPIC_TITLE_KEYS` sabiti MapControls'ta kullanılır — oraya taşınır. `topicMeta` hesaplaması tuvalde (pin rengi + lejant için hem MapCanvas hem MapControls gerekir).

## Korunacaklar (Zero-Loss §5.5)

1. **Playback logic**: interval 1500ms, revealedCount=0 başlat, sona ulaşınca dur — aynen
2. **Drag**: pointer capture, revealedCount oranı, min/max clamp — aynen
3. **Step**: ileri/geri clamp — aynen
4. **Fullscreen**: container fullscreen + fullscreenchange dinleyicisi — aynen
5. **SVG**: pin pulsing animasyonu + `mapPulse` keyframes + lejant + bilgi kartı — aynen
6. **i18n**: `t[TOPIC_TITLE_KEYS[selectedTopic]]`, `t[\`kpss_map_topic_${topic.id}\`]` dinamik anahtarlar — aynen

## Doğrulama

```
npx tsc --noEmit
npx eslint src --quiet
npm run build
node scripts/findDeadFiles.mjs   → "Toplam: 0 dosya"
```

Ardından: `src/ARCHITECTURE.md` güncelle (§7.1) + mermaid (§7.2).

## Açık Sorular

> [!NOTE]
> `MapControls` ~150 satır, `MapCanvas` ~160 satır — ikisi de limit içinde. Tuval ~180 satır. Hepsi ok.

> [!NOTE]
> `TurkeyMapView` import'larını kimler yapıyor? `KpssView.tsx` → büyük ihtimal. Split `TurkeyMapView` export adını SAME tutacak → import kırılmaz. Taranacak.
