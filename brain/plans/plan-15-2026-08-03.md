# Plan 15 — PopupPomoTab.tsx Bölme (566 → ~80 satır)

**Tarih**: 2026-08-03
**Dosya**: [src/components/popup/PopupPomoTab.tsx](file:///c:/Users/emre_/Desktop/GitHub/Done/chrome-extension/src/components/popup/PopupPomoTab.tsx)
**Kural**: §6.1 Sorumluluk Esaslı

---

## Problem

`PopupPomoTab.tsx` **566 satır**, 4 ayrı panel:
- Pomodoro paneli (mod seçici + timer circle + kontroller) — L134-306
- Stopwatch paneli (senkronize) — L307-407
- Alarmlar paneli (telefon stili + hızlı ekleme + liste) — L408-550
- Odak müzikleri mini kartı — L551+

## Hedef Mimari

```
src/components/popup/pomo/
├── PopupPomoTab.tsx      → tuval (~80) — state + 3 panel kompozisyonu
├── PomoTimerPanel.tsx    → parça (~170) — timer circle + modlar + kontroller
├── PomoStopwatchPanel.tsx → parça (~100) — stopwatch
├── PomoAlarmsPanel.tsx   → parça (~140) — alarm listesi + ekleme formu
└── PomoFocusSounds.tsx   → parça (~60) — odak müzikleri
```

## Korunacaklar
- `audioEngineRef` mantığı — tuvalde kalır (paylaşılan)
- Timer circle SVG progress — aynen
- Alarm ekleme/silme + telefon listesi — aynen
- Odak müzikleri kartı — aynen

## Doğrulama
`npx tsc --noEmit` → `npx eslint src --quiet` → `npm run build` → `node scripts/findDeadFiles.mjs`
