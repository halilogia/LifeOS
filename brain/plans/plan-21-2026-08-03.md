# Plan 21 — KPSS İstatistik Kartları Bölme (446+445+430+406)

**Tarih**: 2026-08-03
**Dosya**: KpssDailyStatsCard, KpssQuestionCanvas, KpssAutoPlannerCard, KpssNetEstimationCard
**Kural**: §6.1 Sorumluluk Esaslı

---

## Problem

4 KPSS kartı şişkin, her biri tek TEMA ama 2+ UI bloğu:

| Dosya | Satır | Bloklar |
|---|---|---|
| KpssDailyStatsCard | 446 | metrik filtre + aralık butonları + kayıtlı log çipleri |
| KpssQuestionCanvas | 445 | chart config + efekt (L20-432) + render |
| KpssAutoPlannerCard | 430 | stats bar + bilgi modalı + açıklama (L289-408) |
| KpssNetEstimationCard | 404 | hedef takip + durum göstergesi + progress bar + konu kartları |

## Hedef Mimari

```
src/components/kpss/parts/
├── KpssDailyStatsCard.tsx   → tuval (~200) — state + 3 parça
├── KpssMetricPills.tsx      → parça (~60) — filtre pill'leri
├── KpssSavedLogChips.tsx    → parça (~80) — kayıt çipleri + silme

├── KpssAutoPlannerCard.tsx  → tuval (~200) — plan + dark
├── PlannerStatsBar.tsx      → parça (~60) — istatistik bar
├── PlannerInfoModal.tsx     → parça (~110) — açıklama modalı

├── KpssNetEstimationCard.tsx → tuval (~160)
├── NetTargetSection.tsx     → parça (~100) — hedef + durum + progress
├── NetSubjectCards.tsx      → parça (~80) — konu kartları

├── KpssQuestionCanvas.tsx   → tuval (~120) — chart render
└── ChartOptionBar.tsx       → parça (~60) — chart kontrolleri
```

## Öncelik
- En çok sorumluluk taşıyan: **KpssDailyStatsCard** (3 UI seti) ve **KpssAutoPlannerCard** (2 UI seti + modal)
- **KpssQuestionCanvas** en büyük fakat çoğu chart.js config — tek sorumluluğa yakın, sona bırakılır

## Korunacaklar
- Tüm chart ayarları, hedef hesaplama, progress oranı, konu tıklama — aynen
- Metrik filtre + kayıt silme — aynen

## Doğrulama
`npx tsc --noEmit` → `npx eslint src --quiet` → `npm run build` → `node scripts/findDeadFiles.mjs`
