# C1/C2 Wiring Fix — Presentation Layer Bypass Repair

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** Hooks katmanının `application/use-cases` + `ports` üzerinden akmasını sağlamak; `useCalendar` (C1) ve `useBist` (C2) ihlallerini onarmak.

**Architecture:** 
- **C1:** `useCalendar` → yeni `ICalendarSyncPort` + `SyncGoogleCalendarUseCase` üzerinden Google Calendar'a erişecek. Port, `ITodoSyncPort` ile aynı desende `infrastructure/api` adapter'larını sarar. Sync settings okuması da `ISyncRepository`'e taşınır (hook'taki raw `chrome.storage.local.get` kalkar).
- **C2:** `useBist` → `domain/repositories/IStockRepository.ts` arayüzü + `ChromeStorageStockRepository implements IStockRepository` + `bistService`'in factory'si repo'yu alacak şekilde genişletilir. Hook, use case kullanmaz (BIST'e use case yazmak over-engineering — CRUD handler'lar hook'ta kalır, ama **storage'a tek erişim noktası** repo arayüzü olur).

**Tech Stack:** Preact + TypeScript + Zustand. Clean Arch: `domain/` (interfaces) → `application/ports/` (port'lar) → `application/use-cases/` (orchestration) → `infrastructure/` (chrome.* API'ler) → `presentation/hooks/` (state).

---

## C1 — Calendar Hook Wiring

### Task 1: `ICalendarSyncPort` port arayüzü oluştur

**Objective:** Google Calendar erişimini port üzerinden soyutla.

**Files:**
- Create: `src/application/ports/ICalendarSyncPort.ts`

**Steps:**
1. Aşağıdaki dosyayı yaz (mevcut `ITodoSyncPort` desenine birebir uygun):

```typescript
/**
 * ICalendarSyncPort Interface
 * Port for fetching Google Calendar events.
 * Application layer - defines the boundary between application and infrastructure.
 */

export interface RemoteCalendarEvent {
  readonly start?: { dateTime?: string; date?: string };
  readonly summary?: string;
}

export interface ICalendarSyncPort {
  getAuthToken(interactive: boolean): Promise<string>;
  getCalendarEvents(
    token: string,
    start: string,
    end: string,
  ): Promise<RemoteCalendarEvent[]>;
}
```

2. **Verify:** `npx tsc --noEmit` → exit 0

### Task 2: `createCalendarPort` factory adapter oluştur

**Objective:** Port'u infrastructure API'lerine bağlayan composition-root factory.

**Files:**
- Create: `src/application/ports/createCalendarPort.ts`

**Steps:**
1. `createSyncPort.ts` desenini kopyala (auth + API sarımı):

```typescript
import type { ICalendarSyncPort } from "@/application/ports/ICalendarSyncPort.js";
import { GoogleAuthApi } from "@/infrastructure/api/GoogleAuthApi.js";
import { GoogleCalendarApi } from "@/infrastructure/api/GoogleCalendarApi.js";

/**
 * Creates an ICalendarSyncPort instance backed by GoogleAuthApi and GoogleCalendarApi.
 * Called once in the composition root and injected into use cases.
 */
export function createCalendarPort(): ICalendarSyncPort {
  const authApi = new GoogleAuthApi();
  const calendarApi = new GoogleCalendarApi();

  return {
    getAuthToken: (interactive) => authApi.getAuthToken(interactive),
    getCalendarEvents: (token, start, end) =>
      calendarApi.getCalendarEvents(token, start, end),
  };
}
```

2. **Verify:** `npx tsc --noEmit` → exit 0

### Task 3: `SyncGoogleCalendarUseCase` use case oluştur

**Objective:** Calendar fetch orkestrasyonunu application katmanına taşı. Hook'taki `chrome.storage.local.get(["syncEnabled","syncCalendarEnabled"])` + `_authApi` + `_calendarApi` + try/catch mantığı buraya gelir.

**Files:**
- Create: `src/application/use-cases/sync/SyncGoogleCalendarUseCase.ts`

**Steps:**
1. Use case'i yaz:

```typescript
import type { ISyncRepository } from "@/domain/repositories/ISyncRepository.js";
import type { ICalendarSyncPort } from "@/application/ports/ICalendarSyncPort.js";
import { logger } from "@/utils/logger.js";

export interface CalendarSyncResult {
  events: { start?: { dateTime?: string; date?: string } }[];
}

export class SyncGoogleCalendarUseCase {
  constructor(
    private syncRepository: ISyncRepository,
    private calendarPort: ICalendarSyncPort,
  ) {}

  async execute(year: number, month: number): Promise<CalendarSyncResult> {
    const settings = await this.syncRepository.getSyncSettings();
    if (!settings.enabled || !settings.calendarEnabled) {
      return { events: [] };
    }
    try {
      const token = await this.calendarPort.getAuthToken(false);
      const startStr = new Date(year, month, 1, 0, 0, 0).toISOString();
      const endStr = new Date(year, month + 1, 0, 23, 59, 59).toISOString();
      const events = await this.calendarPort.getCalendarEvents(
        token,
        startStr,
        endStr,
      );
      return { events };
    } catch (e) {
      logger.error("[CalendarView] Google Calendar fetching error:", e);
      return { events: [] };
    }
  }
}
```

> **Not:** `syncSettings.enabled && syncSettings.calendarEnabled` kontrolü use case'e taşındı — bu iş mantığı, hook'ta kalmamalı. `calendarEnabled` alanının `GoogleSyncSettings` tipinde mevcut olduğunu `GoogleAuthUseCase.ts:32` doğruluyor.

2. **Verify:** `npx tsc --noEmit` → exit 0

### Task 4: `useCalendar.ts` hook'unu use case'e bağla

**Objective:** Hook'tan infrastructure import'larını sil, use case + port kullan.

**Files:**
- Modify: `src/presentation/hooks/useCalendar.ts`

**Steps:**
1. Import'ları değiştir:
   - Sil: `GoogleAuthApi`, `GoogleCalendarApi` import'ları, `_authApi`/`_calendarApi` singleton'ları (satır 3-4, 7-8)
   - Ekle: `SyncGoogleCalendarUseCase`, `createCalendarPort`, `ChromeStorageSyncRepository`
2. Module-level composition (syncStore.ts deseni):

```typescript
import { SyncGoogleCalendarUseCase } from "@/application/use-cases/sync/SyncGoogleCalendarUseCase.js";
import { createCalendarPort } from "@/application/ports/createCalendarPort.js";
import { ChromeStorageSyncRepository } from "@/infrastructure/persistence/repositories/ChromeStorageSyncRepository.js";

const syncRepo = new ChromeStorageSyncRepository();
const calendarUC = new SyncGoogleCalendarUseCase(syncRepo, createCalendarPort());
```

3. `useEffect` içindeki `fetchCalendar` fonksiyonunu değiştir — raw storage read + API çağrısı yerine:

```typescript
const fetchCalendar = async () => {
  const { events } = await calendarUC.execute(year, month);
  if (isMounted) {
    setCalendarEvents(events);
  }
};
```

> **Sonuç:** Hook'ta `chrome.*` çağrısı sıfır. `calendarEvents` state tipi use case dönüşüyle uyumlu (`{ start?: { dateTime?: string; date?: string } }[]`) — consumer `CalendarView.tsx` değişmez.

4. **Verify:** `npx tsc --noEmit` → exit 0
5. **Verify:** `grep -rn "GoogleCalendarApi\|GoogleAuthApi" src/presentation/hooks/useCalendar.ts` → boş

### Task 5: C1 doğrulama

**Steps:**
1. `npx tsc --noEmit` → exit 0
2. `npm run build` → exit 0
3. `git diff --stat` → yalnızca: `useCalendar.ts`, 3 yeni dosya (port, factory, use case)
4. Commit: `refactor(calendar): wire useCalendar through SyncGoogleCalendarUseCase + ICalendarSyncPort`

---

## C2 — BIST Hook Wiring

### Task 6: `IStockRepository` domain arayüzü oluştur

**Objective:** BIST persistence'ını domain interface'e bağla. Hook artık `ChromeStorageStockRepository` yerine arayüze bağımlı.

**Files:**
- Create: `src/domain/repositories/IStockRepository.ts`

**Steps:**
1. `ChromeStorageStockRepository`'nin tüm public metotlarını arayüzle (import'lar `@/types/stock.js`):

```typescript
import type {
  StockPortfolioItem,
  StockRule,
  StockAlertLog,
  StockWatchlist,
  StockTradeHistory,
  StockCashBalance,
} from "@/types/stock.js";

export interface IStockRepository {
  getPortfolio(): Promise<StockPortfolioItem[]>;
  savePortfolio(portfolio: StockPortfolioItem[]): Promise<void>;
  getRules(): Promise<StockRule[]>;
  saveRules(rules: StockRule[]): Promise<void>;
  getAlertLogs(): Promise<StockAlertLog[]>;
  saveAlertLogs(logs: StockAlertLog[]): Promise<void>;
  addAlertLog(log: StockAlertLog): Promise<void>;
  getTradeHistory(): Promise<StockTradeHistory[]>;
  saveTradeHistory(items: StockTradeHistory[]): Promise<void>;
  addTradeHistory(item: StockTradeHistory): Promise<void>;
  getCashBalance(): Promise<StockCashBalance>;
  setCashBalance(balance: StockCashBalance): Promise<void>;
  getWatchlists(): Promise<StockWatchlist[]>;
  saveWatchlists(watchlists: StockWatchlist[]): Promise<void>;
  createWatchlist(name: string, description?: string): Promise<StockWatchlist[]>;
  deleteWatchlist(id: string): Promise<StockWatchlist[]>;
  toggleSymbolInWatchlist(
    watchlistId: string,
    symbol: string,
  ): Promise<StockWatchlist[]>;
}
```

2. **Verify:** `npx tsc --noEmit` → exit 0

### Task 7: `ChromeStorageStockRepository implements IStockRepository`

**Files:**
- Modify: `src/infrastructure/persistence/repositories/ChromeStorageStockRepository.ts:33`

**Steps:**
1. Import ekle: `import type { IStockRepository } from "@/domain/repositories/IStockRepository.js";`
2. Class satırını değiştir:

```typescript
export class ChromeStorageStockRepository implements IStockRepository {
```

3. **Verify:** `npx tsc --noEmit` → exit 0 (metot imzaları zaten birebir aynı — saf `implements` eklemesi)

### Task 8: `bistService` factory'sini repo'ya bağla

**Objective:** `createBistService` cache repo'sunun yanında `IStockRepository` alsın; hook tek composition noktasından beslenir.

**Files:**
- Modify: `src/services/bistService.ts:187`

**Steps:**
1. Mevcut `createBistService(cacheRepo: IBistCacheRepository)` imzasını incele (satır 187-239)
2. Factory'ye ikinci parametre ekle: `createBistService(cacheRepo: IBistCacheRepository, stockRepo: IStockRepository)`
3. Factory içindeki metotlar `chrome.storage` yerine `stockRepo` çağırsın (portfolio/watchlist/rule/log metotları varsa)
4. Singleton'ı güncelle:

```typescript
const stockRepo = new ChromeStorageStockRepository();
export const { fetchStockQuote, fetchStockPrices, refreshStockPrices } =
  createBistService(new ChromeStorageBistCacheRepository(), stockRepo);
```

> **Dikkat:** `bistService.ts:239`'daki destructuring export'u koru — consumer'lar `fetchStockQuote` vb. import ediyor. Factory'nin dönüş şekli değişmemeli, yalnızca **içindeki storage erişimi** repo'ya yönlenmeli.

5. **Verify:** `npx tsc --noEmit` → exit 0

### Task 9: `useBist.ts` hook'unu repo arayüzüne bağla

**Objective:** Hook'taki doğrudan `new ChromeStorageStockRepository()` kaldır, arayüz tipiyle bağlan.

**Files:**
- Modify: `src/presentation/hooks/useBist.ts:5,22`

**Steps:**
1. Import'u değiştir (satır 5):

```typescript
import type { IStockRepository } from "@/domain/repositories/IStockRepository.js";
```

2. Singleton tipini değiştir (satır 22):

```typescript
const stockRepository: IStockRepository = new ChromeStorageStockRepository();
```

> **Not:** Concrete class'ı `new`'lemek presentation'da kabul edilebilir (composition root deseni — `syncStore.ts` de aynısını yapıyor: `new ChromeStorageTodoRepository()` + use case'e enjekte). Kritik olan, hook'un **tipinin** arayüz olması — testlerde mock kolaylaşır, ileride DI container'a taşınabilir.

3. **Verify:** `npx tsc --noEmit` → exit 0
4. **Verify:** `grep -rn "ChromeStorageStockRepository" src/presentation/` → useBist.ts'te yalnızca singleton satırı

### Task 10: C2 doğrulama

**Steps:**
1. `npx tsc --noEmit` → exit 0
2. `npm run build` → exit 0
3. Commit: `refactor(bist): bind useBist to IStockRepository domain interface`

---

## Genel Doğrulama (Tüm Task'lar Sonrası)

```bash
npx tsc --noEmit          # exit 0
npm run lint              # exit 0 (180s+ olursa background + notify_on_complete)
npm run build             # exit 0
npm run test              # exit 0 (mevcut cloudBackup testleri)
```

**İhlal öncesi/sonrası kontrol:**
```bash
grep -rn "from \"@/infrastructure" src/presentation/hooks/  # useCalendar, useBist temiz olmalı
```

---

## Değişecek Dosyalar (Özet)

| Dosya | İşlem |
|---|---|
| `src/application/ports/ICalendarSyncPort.ts` | Yeni |
| `src/application/ports/createCalendarPort.ts` | Yeni |
| `src/application/use-cases/sync/SyncGoogleCalendarUseCase.ts` | Yeni |
| `src/presentation/hooks/useCalendar.ts` | Modifiye (use case'e bağla) |
| `src/domain/repositories/IStockRepository.ts` | Yeni |
| `src/infrastructure/persistence/repositories/ChromeStorageStockRepository.ts` | Modifiye (`implements`) |
| `src/services/bistService.ts` | Modifiye (factory repo bağla) |
| `src/presentation/hooks/useBist.ts` | Modifiye (arayüz tipi) |

**Consumer'lar (DOKUNMA):** `CalendarView.tsx`, tüm `useBist` consumer'ları — hook dönüş şekli değişmiyor.

---

## Riskler & Açık Sorular

1. **`calendarEnabled` alanı:** `ISyncRepository.getSyncSettings()` dönüşünde mevcut (`GoogleAuthUseCase.ts:32` kanıtı). Kullanım sırasında doğrula.
2. **`bistService` factory içeriği:** Task 8'de factory'nin içinde portfolio/watchlist metotları **yoksa** (yalnızca cache/fetch metotlarıysa), factory imza değişikliği gereksiz olabilir — bu durumda Task 8'i atla ve yalnızca Task 9'u yap. Factory'nin mevcut gövdesini oku, karar ver.
3. **`useAppInit.ts:80`** zaten `new SyncGoogleTasksUseCase(todoRepo, syncRepo, syncPort)` yapıyor — bu desen onaylı. Yeni calendar use case'i de aynı desende.
4. **Commit sıklığı:** Her task sonunda commit (plan skill'i kuralı). Kullanıcı commit istemiyorsa atla.

**Tahmini kapsam:** ~220 satır yeni kod, ~40 satır değişiklik. Consumer'da sıfır değişiklik.
